import default

def has_two_coordinates(container) :
    return len(container) == default.n_dimensions

def is_valid_axis_index(i_axis) :
    return i_direction == default.index_u or i_direction == default.index_v
